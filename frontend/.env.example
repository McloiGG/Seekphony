# Seekphony frontend local development configuration.
# Copy to frontend/.env.local for Vite dev runs.
# Browser-exposed values are public and must not contain secrets.

VITE_API_BASE_URL=http://127.0.0.1:8000

# Used by the frontend Docker image at container startup.
SEEKPHONY_PUBLIC_API_BASE_URL=http://localhost:8000
